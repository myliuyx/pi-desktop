# 设计稿回写差异单（design-writeback · R7–R9）· 已执行

> **状态：已执行完成（2026-09-22 晚）**。由具备 Ardot 画布工具的会话照单执行，
> 原 `design-writeback-pending.md` 的 4 项待回写逐条落实，执行记录见文末。
> 画布：Ardot fileId `728255468414716`。执行原则遵循 `.plan/figma-reverse-sync-status.md`：
> ① 只动属性/变量，不动图层结构；② 跨两端命题须两端观测——回写后画布截图须与实现终态截图比对。

## 背景

2026-09-22 微调轮（R7–R9）产生了 4 项布局定稿，属于**实现侧领先于设计稿**的差异。实现终态截图：
`.workbuddy/shots/r7-default.png`（双展开）、`.workbuddy/shots/r7-collapsed.png`（折叠态）。
裁决依据全文见 `.plan/ui-rulings.md`。

## 回写结果（4/4）

### 1. 侧边栏历史会话条目 → 两行式 ✅

- 结构核对：01 浅色 / 02 深色的 5 个会话项**本就是两行式**（第一行标题、第二行元信息、左对齐），无结构改动。
- 文案回写：第二行由旧格式「12 步 · 3 分钟前」等，改为实现侧定稿格式「相对时间 N 条消息」
  （`Sidebar.tsx`：`${formatRelativeTime} ${messageCount} 条消息`，锚定 `SESSION_LIST_NOW`）：
  - `3小时前 7 条消息`（活动项）
  - `8小时前 292 条消息` / `19小时前 63 条消息` / `22小时前 100 条消息` / `23小时前 102 条消息`
- 涉及节点：3:182/3:185/3:188/3:191/3:194（浅色）、3:4083/3:4085/3:4087/3:4089/3:4091（深色）。
- 备注（非本次范围）：画布会话**标题**与实现 mock 标题不一致（画布为自有示例文案），
  属内容层差异，未动 —— 如需两端逐字对齐可另开内容同步单。

### 2. 标题栏右侧预览区收起/展开控件 ✅（核对通过，无改动）

- 01 浅色（3:3960「收起左侧」/ 3:3965「收起右侧」）与 02 深色（3:3985 / 3:3993）**均已存在两个独立控件**，
  与实现 `TitleBar.tsx` 的 `titlebar-toggle-sidebar` / `titlebar-toggle-preview` 一一对应；
  语义即 R5 裁决的「只收右侧」，无「收起两侧」残留，无需改动。

### 3. 会话消息列 720px 居中 ✅

- 回写方式（不动结构）：消息流容器左右 padding 由 20 改为 **67**（= (854 − 720) / 2，
  主区宽 854 = 1440 − 264 侧栏 − 2 分隔线 − 320 上下文栏），内容盒恰为 720px 且在会话区内水平居中；
  节点更名为「消息流（720 内容列·居中）」自注释。
- 涉及节点：3:213（浅色）、3:4015（深色）。
- **输入区未动**：保持全宽（R8a 刻意裁决，与消息列居中构成刻意不对称）——执行时已确认未触碰。

### 4. 我方消息气泡贴右、不占满 ✅

- 回写：用户消息行 `primaryAxisAlignItems: MAX`（贴右、右缘 0 间距）；用户气泡及内部文字
  `width: hug_contents`（内容自适应；720 封顶由父级 720 列天然约束）。
- 对方（assistant）气泡保持占满列宽，未动。
- 涉及节点：3:214 / 3:215 / 3:216（浅色）、3:4054 / 3:4106 / 3:4148（深色）。

## 明确不回写（防误操作，本次已遵守）

- **发送按钮纸飞机图标 0.5px 光学补偿**：实现层像素细节，设计稿矢量保持几何居中，未回写（R9）。
- **Theme 令牌 23 项 × 2 模式**：2026-09-22 早间反向同步已完成，未重做。
- **`--text-tertiary` 2.93:1**：设计稿定稿值未动（R10 已接受为已知例外）。

## 执行后动作（已完成）

1. **布局校验**：`capture_layout(problemsOnly)` 两屏均无溢出/裁切；消息流内容盒实测
   local x=67、宽 720，即 720 居中列成立。剩余 warning 均为既有意图区（消息流底部留白、
   头部/侧栏弹性占位、预览卡片留白），非缺陷。
2. **两端观测**：画布截图（01 浅色 / 02 深色）与 `.workbuddy/shots/r7-default.png` 比对——
   元信息格式、720 居中列、我方气泡贴右、输入区全宽四点形态一致 ✅。
   已知差异：mock 文案（会话标题、消息内容）两端不同源，不属布局定稿范围。
3. 本文档移入 `.plan/diffs/`，MEMORY.md 已销账。
