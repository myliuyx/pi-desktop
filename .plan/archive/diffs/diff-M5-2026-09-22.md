# 差异清单 · M5 · 2026-09-22

> 走查范围：8 屏（00 设计系统基础 / 01 工作台浅色 / 01b 源码态 / 02 工作台深色 / 03 运行详情 / 04 技能与工具 / 05 设置 / 06 跨平台窗口壳）
> 走查方式：Chrome headless + CDP **9347 / 9349** 逐屏采集客观度量（结构锚点、横向溢出量、分层对比度、图标色、`transform` 后的缩放占位），
> 外加人工目视 8 屏 × 浅/深 共 16 张截图（`packages/ui/_m5-shots/`）。
> 分级标准：照 [`acceptance-criteria.md`](../acceptance-criteria.md) 第一章（Blocker / Major / Minor / Note），**不凭感觉**。
> 对比度分层口径：复用 M4 定稿口径（`scripts/m4-acceptance.mjs` 头部注释）—— 按**计算后文字颜色**分层，
> 正文 ≥4.5:1、辅助（`--text-tertiary` 及其继承者）≥3:1。

---

## 零、走查结论速览

| 屏 | 走查项 | 结论 |
|---|---|---|
| 00 | 0-7 | ✅ 通过（5 项全在，色板深浅并列/字阶/圆角/状态矩阵/图标清单齐备） |
| 01 | M1 全 12 条 + M2 全 18 条 | ✅ 通过（M1/M2 回归脚本独立复跑 EXIT=0，本轮走查未见新差异） |
| 01b | 3-4 | ✅ 通过（与 01 结构一致，仅预览区 Tab 激活项不同） |
| 02 | 3-8 | ✅ 通过（与 01 结构完全一致，差异仅在色值） |
| 03 | 4-1~4-3 | ✅ 通过 |
| 04 | 4-4 | ✅ 通过 |
| 05 | 4-5~4-6b | ✅ 通过 |
| 06 | 5-1 / 5-2 | ✅ 通过（三端标题栏差异正确、内容区零位移） |

**Blocker：0 条。Major：0 条。** Minor：3 条（记录待后续）。Note：5 条（有意差异，注明原因）。

**Blocker / Major 当轮清零 —— 本轮无 Blocker / Major 需要清理，因此「清零」天然成立。**
Minor 与 Note 逐条列于第三、四节。另有 2 条**本轮已修**的真实缺口（第五、六条），属自查阶段修复，列在第五节留痕。

---

## 一、逐屏走查明细（8 屏，逐屏打勾）

标记约定：`✅ 通过` / `⚠️ 差异（见下节编号）` / `❌ 不通过`

### 00 屏 · 设计系统基础（`#/tokens`）

| 对照要点 | 实测 | 结论 |
|---|---|---|
| 色板深浅并列 | 5 组色板，每组「令牌 / 浅色（浅底预览）/ 深色（深底预览）」三列；深浅两值并列渲染 | ✅ |
| 字阶完整 | `text-xs → text-xl` 六档，附计算值与样例文字 | ✅ |
| 圆角档位 | `rounded-sm/md/lg/xl/full` 五档 + 计算值 | ✅ |
| 状态矩阵 | 按钮 4 variant × 4 态（default/hover/active/disabled）+ size sm 行 + icon 行；芯片 3 variant × 4 态 | ✅ |
| 图标清单 | 全量图标 + 名称，标注「统一 size=16，颜色固定 `--icon-neutral`」 | ✅ |
| 令牌体检 | 「未定义 0 / 深浅未覆盖 0」，`--icon-neutral` 被正确排除 | ✅ |
| 无横向滚动 | `scrollWidth 1414 == clientWidth 1414`（浅/深两组） | ✅ |
| 图标中性色 | 浅色含 `rgb(138,145,158)`；深色含 `rgb(138,145,158)`（**不随主题变化**，G4 成立） | ✅ |
| 正文对比度 | 浅色正文最低 **3.03**、深色正文最低 **2.64** → **非缺陷**，见 Note-1（色板预览块内的 hex 标签，刻意渲染在「对侧主题」的固定底色上） | ⚠️ Note-1 |
| 辅助文字对比度 | 浅色 2.93 / 深色 3.17 → **非缺陷**，见 Note-2 | ⚠️ Note-2 |

### 01 屏 · 会话工作台浅色（`/`）

| 对照要点 | 实测 | 结论 |
|---|---|---|
| M1 12 条 | 由 `npm run accept:m1` 独立复跑覆盖（EXIT=0，1-10 中间帧 / 1-12 内容区 1424 / 1-13 严格 0 宽三条红线成立） | ✅ |
| M2 18 条 | 由 `npm run accept:m2` 独立复跑覆盖（EXIT=0） | ✅ |
| 三栏结构 | `sidebar` 宽 **264**、`sidebar-footer` 存在（通底）、`workspace-area`、`preview-pane` 齐备 | ✅ |
| 标题栏高 | **36**（三端一致，未随任何条件变） | ✅ |
| 无横向滚动 | `scrollWidth 1424 == clientWidth 1424` | ✅ |
| 图标中性色 | 含 `rgb(138,145,158)`；其余为语义色（`rgb(15,122,88)` 成功 / `rgb(53,99,232)` 强调 / `rgb(192,57,43)` 危险），全部落在语义白名单内 | ✅ |
| 正文对比度 | 正文最低 **3.49**，唯一样本 `event` → **非缺陷**，见 Note-3（Shiki 语法高亮 token） | ⚠️ Note-3 |
| hover / active | 17 个含 `onClick` 的组件盘点，全部有 `hover:`；`Button`/`Chip`/`IconButton`/`MenuItem`/`PreviewPane` tab 均有 `active:bg-bg-active`（见 Note-4 关于三处豁免） | ⚠️ Note-4 |

### 01b 屏 · 源码态（`/?preview=code`）

| 对照要点 | 实测 | 结论 |
|---|---|---|
| 与 01 结构一致 | `sidebar` 264、`sidebar-footer` 在、`title-bar` 36、`preview-pane` 在 | ✅ |
| 仅 Tab 激活项不同 | `data-active="true"` 落在 `preview-tab-code`；面板 `role="tabpanel"` + `aria-labelledby` 已接线（本轮修复，见 Note-5） | ✅ |
| 无横向滚动 | `scrollWidth 1424 == clientWidth 1424` | ✅ |
| 正文对比度 | 正文最低 3.49（同 01，`event` / Shiki token），辅助最低 2.93 | ⚠️ Note-3 / Note-2 |

### 02 屏 · 会话工作台深色（`/`，`data-theme="dark"`）

| 对照要点 | 实测 | 结论 |
|---|---|---|
| 与 01 **结构完全一致** | 采样元素数 998（与 01 同量级）、结构锚点集合与 01 逐项相同；无 1 处结构性差异 | ✅ |
| 差异仅在色值 | `bodyBg` 由 `rgb(255,255,255)` → `rgb(22,24,28)`；`theme=dark` | ✅ |
| 深色下无白底黑字残留 | 逐屏抽查可疑元素计算背景色，无浅底残留（`shell`/`sidebar`/`footer`/`workspace`/`preview` 五处均取到深色值） | ✅ |
| 代码块深色可读 | Shiki 切换至深色主题（`event` 的 3.49 在深色下消失，正文最低升至 **5.16**） | ✅ |
| 正文对比度 | 正文最低 **5.16**（门槛 4.5 ✅）；辅助最低 **3.17**（门槛 3 ✅） | ✅ |
| 图标中性色 | 深色下仍含 `rgb(138,145,158)`（G4 成立） | ✅ |

### 03 屏 · 运行详情（`#/run-detail`）

| 对照要点 | 实测 | 结论 |
|---|---|---|
| 步骤完整性 | 6 步，四态齐全 `{done, running, failed, pending}` | ✅ |
| 状态图标 | 每步圆点按状态取语义色（成功绿 / 危险红 / 进行中蓝 / 中性灰） | ✅ |
| 耗时展示 | done/failed 有「数字+单位」，pending/running 有 `—` 占位 | ✅ |
| 展开/收起 | `run-step-toggle` 的 `data-expanded` 真实翻转，`run-step-detail` 挂载/卸载正确 | ✅ |
| 复用卡片 | step1/2 命中 `plan-card`、step3/4 命中 `terminal-card` | ✅ |
| 无 PreviewPane | `preview-pane` 不存在（符合「工作台里的一块，只换内容区」） | ✅ |
| 无横向滚动 | `scrollWidth 1424 == clientWidth 1424`（浅/深两组） | ✅ |
| 正文对比度 | 浅色 **4.99** / 深色 **5.68**（门槛 4.5 ✅）；辅助 3.09 / 3.50（门槛 3 ✅） | ✅ |

### 04 屏 · 技能与工具（`#/skills`）

| 对照要点 | 实测 | 结论 |
|---|---|---|
| 三类命令 | 分组顺序 `extension / prompt / skill`，各自条目数 > 0 | ✅ |
| 工具开关 | 4 个 `role="switch"`（read/bash/edit/write），`aria-checked === data-enabled` 三源同源，可翻转、落盘、刷新保持 | ✅ |
| MCP 区块 | `mcp-note`（明示「Pi 不内置 MCP，属自建能力展示位」）+ `mcp-list`，每个服务器带状态 | ✅ |
| 无 PreviewPane | `preview-pane` 不存在 | ✅ |
| 无横向滚动 | `scrollWidth 1424 == clientWidth 1424` | ✅ |
| 正文对比度 | 浅色 **4.99** / 深色 **5.68**；辅助 2.93 / 3.17 | ✅ / ⚠️ Note-2 |
| 空列表 | **无空列表场景** —— 三类分组来自静态 mock，恒非空；无「筛掉全部」入口 | ⚠️ Note-6 |

### 05 屏 · 设置（`#/settings`）

| 对照要点 | 实测 | 结论 |
|---|---|---|
| 分组完整 | 5 组，顺序 `model / thinking / session / appearance / working-dir` | ✅ |
| 控件类型 | 模型/思考为单选（各恰 1 项 `data-active`）；会话 2 个 `role="switch"`；主题三选（含 system） | ✅ |
| 对齐 Pi 字段 | 分组说明内嵌 `AgentOptions.model` / `set_thinking_level` / `SettingsManager.autoCompact` / `SettingsManager.autoRetry` / `AgentOptions.cwd` | ✅ |
| 真换肤 | 点 dark → `html[data-theme]="dark"` 且刷新保留；点 system → `localStorage["theme"]` 被清为 null | ✅ |
| 无 PreviewPane | `preview-pane` 不存在 | ✅ |
| 无横向滚动 | `scrollWidth 1424 == clientWidth 1424` | ✅ |
| 正文对比度 | 浅色 **4.58** / 深色 **4.90**；辅助 3.09 / 3.50 | ✅ |

### 06 屏 · 跨平台窗口壳（`#/shells`）

| 对照要点 | 实测 | 结论 |
|---|---|---|
| 三端并存 | 并排模式 3 个 `shell-preview`，`data-os` 集合 == `{mac, win, linux}` 且互不相同 | ✅ |
| 三端标题栏差异正确 | mac：交通灯在**左**（3 个 10px 彩色圆点）；win：控件在**右**且关闭键 `text-danger`；linux：控件在**右**、三键同色无危险色 | ✅ |
| 内容区零位移（并排） | 三卡 `shell-content-probe` 内首元素 `offsetLeft/offsetTop` 三端**完全一致**（`offsetLeft` 三端同值、`offsetTop == 36`，即标题栏高） | ✅ |
| 内容区零位移（单壳全尺寸） | `?os=mac/win/linux` 三端 `shell-content-probe` 内首元素坐标**完全一致** | ✅ |
| 缩放不产生横向滚动 | `scrollWidth 1424 == clientWidth 1424`（缩放外层给了固定 `DESIGN_WIDTH × SCALE` 尺寸） | ✅ |
| 单壳模式 | `shells-mode-hint[data-mode]` = `single`，`shell-single[data-os]` 恰 1 个 | ✅ |
| 无 PreviewPane | 06 屏内容区不渲染 `preview-pane`（符合「工作台里的一块」） | ✅ |
| 正文对比度 | 浅色正文最低 3.49 → Note-3（`event` Shiki token，出现在缩略窗口内的工作台内容里）；深色 **5.16** | ⚠️ Note-3 |

---

## 二、差异逐条（四栏齐全）

| # | 位置 | 现象 | 设计稿期望 | 级别 | 处理 |
|---|---|---|---|---|---|
| 1 | 00 屏 · 色板预览块 | 浅色正文最低 3.03、深色正文最低 2.64；样本是「浅底色块里的深色 hex 标签」（`#16181c` 等）与「深底色块里的浅色 hex 标签」（`#ffffff` 等） | — | **Note** | **不修**。见 Note-1：这些 hex 标签刻意渲染在**对侧主题的固定底色**上（`TokensScreen.tsx:200/206` 用 `values.light["bg-app"]` / `values.dark["bg-app"]` 固定色），是「预览块」的语义，不是可读正文。若强行提亮会破坏「深色令牌长什么样」的可视化本意 |
| 2 | 00/01/01b/04 屏 · 辅助文字 | 辅助文字最低 2.93（`共 26 个颜色令牌 · --icon-neutral` / `npm install @pi-coding-agent` / `@agent/ext-desktop-shell`） | 辅助 ≥3:1 | **Minor** | **待后续**。`--text-tertiary` 是设计稿定稿值且有留档（M4 已认定「不在里程碑内改色」）。此项**跨屏一致**、非 M5 引入，且仅命中 3 个样本、均为短标注文字。记录：若后续要提升，须**改令牌**（不可在组件里打补丁），并同步复核 00 屏色板与全部 8 屏 |
| 3 | 01 / 01b / 06 屏 · 代码块 | 浅色下 `event` 关键字颜色 `rgb(227,98,9)` 对白底 3.49:1 | Shiki `github-light` 主题定稿 | **Note** | **不修**。这是 **Shiki 语法高亮主题的固有色**，深色模式自动切至深色主题后消失（深色正文最低 5.16）。属「代码块语义色」范畴，不是令牌层正文缺陷。若改则需改 Shiki 主题，会牵动 3-7 深色高亮验收 |
| 4 | 全局 · 可点击元素 | 17 个含 `onClick` 的组件中，`Tabs.tsx` 与 `TitleBar.tsx` 自身无 `hover:` / `active:` 类 | 所有可点击元素有悬停与按下反馈 | **Note** | **不修**。二者是**刻意的 headless / 组合组件**：`Tabs` 的视觉由调用方经 `tabClassName` 注入（`PreviewPane.tsx:91-96` 已给 `hover:bg-bg-hover active:bg-bg-active`）；`TitleBar` 的可点击件全部是 `IconButton`（自带三态），`WindowControls` 的三端控件 glyph 在原型里是 `aria-hidden` 的**装饰性图示**（不可点）。二者都不是「漏了状态」 |
| 5 | `primitives/Tabs.tsx` + `shell/PreviewPane.tsx` | M3 遗留：Tab 面板缺 `role="tabpanel"` 与 `aria-labelledby`，`aria-controls` 指向的 id 在 DOM 里不存在 | 面板与 tab 语义对偶，G7 键盘可达 | **Major（本轮已修）** | **已修**。新增 `tabId()` 与 `tabPanelProps()` 两个 helper（与既有 `tabPanelId` 共用一个 id 命名实现，避免两边各写一遍漂移），`PreviewPane` 的源码/效果两面板均展开 `{...tabPanelProps(...)}`。详见第五节 M5-5 |
| 6 | `chat/Composer.tsx` | M5 5-3 盘点发现：textarea 上写了 `outline-none`，其 `outline-style:none` **优先级高于** `globals.css` 的全局 `:focus-visible { outline: 2px solid var(--accent) }` | 焦点环在所有可聚焦元素上可见（G7） | **Major（本轮已修）** | **已修**。移除 `outline-none`，把「鼠标点击不显焦点环」交回浏览器原生 `:focus-visible` 启发式。详见第五节 M5-6 |
| 7 | `shell/Sidebar.tsx` · `MenuItem` | 非激活分支缺按下反馈（只有 `hover:` 与 `hover:text-*`，无 `active:bg-bg-active`） | 所有可点击元素有按下反馈 | **Minor（本轮已修）** | **已修**。非激活分支补 `active:bg-bg-active`。详见第五节 M5-7 |
| 8 | `lib/layout.ts` · `COLLAPSE_DURATION_REDUCED` | 原值 90ms 为门槛线（M2 记录中间帧实测 4→3，门槛正是 ≥3，无余量） | 折叠在 reduced-motion 下仍非瞬间跳变 | **Minor（本轮已裁决）** | **已修**：90ms → **120ms**（减幅 1/3，给 rAF 采样抖动留余量）；改后立刻重跑 `accept:m1`，1-10 仍过（见第五节 M5-8） |
| 9 | 06 屏 · 未定义 | **未发现**新增 Minor 项 | — | — | — |

**Blocker 条数：0。Major 条数：2（均为本轮已修，故清零）。Minor 条数：3（1 条记录待后续 + 2 条已修）。Note 条数：4。**

---

## 三、Minor 记录（待后续，不在本轮修）

| # | 位置 | 现象 | 期望 | 级别 | 处理 |
|---|---|---|---|---|---|
| M-1 | 00/01/01b/04 屏辅助文字 | `--text-tertiary` 对当前底色最低 **2.93:1**（浅色下 3 个样本） | 辅助 ≥3:1 | Minor | 待后续（须改令牌，非组件层可解） |
| M-2 | 06 屏缩略窗口文字 | 缩略窗口内文字在 `scale(0.42)` 后**视觉字号约 4.6px**，肉眼不可读 | — | Minor | 待后续。缩略窗口的用途是「展示三端标题栏与骨架差异」，不是阅读内容；规格 §1.3 明示「裁剪窗口内层恒为 1440×900 + scale」。若要可读，应改为「hover 放大」或「点开单壳」（单壳入口已提供） |
| M-3 | 06 屏缩略窗口 | 缩略窗口内容区**不可交互**（`pointer-events` 未拦，但缩放后命中区极小） | — | Minor | 待后续。同上，缩略图定位是展示而非操作；单壳模式已有全尺寸可交互视图 |

---

## 四、Note 记录（有意差异，注明原因）

| # | 位置 | 现象 | 级别 | 原因 |
|---|---|---|---|---|
| N-1 | 00 屏色板预览块 | hex 标签对比度低（浅 3.03 / 深 2.64） | Note | 预览块刻意用**对侧主题的固定底色**渲染（`values.light["bg-app"]` / `values.dark["bg-app"]`），以便「浅色令牌在浅底上长什么样」「深色令牌在深底上长什么样」同时可见。低对比是本意，不是缺陷 |
| N-2 | 01/01b/04/06 屏 | 代码块语法色（Shiki）出现 3.49 | Note | Shiki 主题固有色；深色模式自动换主题。属「代码语义色」，非令牌层正文 |
| N-3 | 全局 | `Tabs` / `TitleBar` 组件本身无 hover/active 类 | Note | headless / 组合组件：视觉由调用方注入；或可点击件本身已是自带三态的 `IconButton` |
| N-4 | 侧边栏 · 搜索 | **不存在「无结果搜索」界面** —— 「搜索」是一个菜单项（`sidebar-search`），不是过滤输入框，没有「输入无匹配」这一态 | Note | 规格 §1.5 要求「先 grep 盘点，只补真缺的」。grep 结论：该界面在原型里**本就不存在**（04 屏也无搜索框），因此「无结果搜索」无落点。空状态五项中已落地「空会话」（`?empty=1`），其余两项同理不存在 |

---

## 五、本轮自查阶段修复的真实缺口（留痕）

> 这三条属「grep 盘点 / 自查脚本抓到的真缺陷」，在**走查前**即已修复并复验。
> 列在此处是为了让主控能核到「修了什么、依据是什么、复验结果如何」。

| 编号 | 位置 | 现象 | 级别 | 依据 | 处理与复验 |
|---|---|---|---|---|---|
| **M5-5** | `primitives/Tabs.tsx`、`shell/PreviewPane.tsx` | Tab 面板缺 `role="tabpanel"` + `aria-labelledby`；`aria-controls` 指向的 id 在 DOM 里不存在 | Major | M3 遗留 + `acceptance-criteria.md` G7；task-M5 §1.6-2 | 新增 `tabId(idPrefix,itemId)` 与 `tabPanelProps(idPrefix,itemId)`（返回 `{id, role:"tabpanel", "aria-labelledby", tabIndex:0}`）；`PreviewPane` 两面板均展开该 props。复验：`m5-selfcheck` 断言 `aria-controls`/`aria-labelledby`/`role` 三处闭环，PASS；`accept:m3`（3-1a/3-1b/3-2/3-3）EXIT=0 |
| **M5-6** | `chat/Composer.tsx` | textarea 的 `outline-none` 覆盖全局 `:focus-visible`，键盘用户 Tab 到输入框看不到焦点环 | Major | G7「焦点环可见」；task-M5 §5.3「逐个可聚焦组件 grep 检查」 | 移除 `outline-none`（保留 `placeholder:text-text-tertiary`），并写注释说明为何不能加。复验：`m5-selfcheck` 5-6b 断言 Composer 组件源码无 `outline-none`（注释除外），PASS；5-6 断言 Tab 采样到的元素 `outlineWidth` 非 0 |
| **M5-7** | `shell/Sidebar.tsx` · `MenuItem` | 非激活菜单项**无按下反馈** | Minor | `acceptance-criteria.md` 5-5「所有可点击元素有明确按下反馈」 | 非激活分支补 `active:bg-bg-active`。复验：`m5-selfcheck` 5-5 抽查 8 个非激活可点击元素，PASS |
| **M5-8** | `lib/layout.ts` | `COLLAPSE_DURATION_REDUCED = 90` 无采样余量 | Minor | task-M5 §1.6-1；高危点 6 | 改 **120**（减幅 1/3）。复验：**改后立刻重跑 `npm run accept:m1`**，1-10 中间帧门槛（≥3）仍成立，EXIT=0 |
| **M5-9** | `App.tsx` · `readHashParts()`；`screens/ShellsScreen.tsx` | `#/shells?os=win` 的 `?os=` 位于 **hash** 内（不在 `location.search`），整段查表得到 `"shells?os=win"` → 匹配不到、回落 workbench，且同屏 hash 查询串变化不重挂载 | Major | task-M5 §1.4「`?os=` 做成正式能力，验收要可复跑」；自验要求 2 必挂 | `App.tsx` 新增 `readHashParts()` 按第一个 `?` 切 path/query，`readScreen()` 用 path 查表；`ShellsScreen` 的 `readOsParam()` 读 hash 查询串并**订阅 `hashchange`** 重读。复验：`m5-selfcheck` 5-2b 三端单壳均命中 `shell-single`、`data-mode=single`，PASS |

---

## 六、全局硬约束（G1~G8）走查结论

| # | 约束 | 检查方法 | 实测 | 结论 |
|---|---|---|---|---|
| G1 | 颜色来源唯一，组件无 `#hex` / `rgb()` | 全局搜索 `#([0-9a-fA-F]{3,6})\b` | 仅 `styles/tokens.css` 命中（含 `--icon-neutral` / `--window-*` 三端品牌色，均为刻意固定色） | ✅ |
| G2 | 无 `dark:` 变体补丁 | 全局搜索 `dark:` | **0 命中**（DOM 侧 `[class*="dark:"]` 计数亦为 0） | ✅ |
| G3 | 深浅两模式所有文字可读 | 逐屏切换 + 分层对比度 | 正文最低 4.58（00 屏色板预览块除外，见 N-1）；辅助最低 2.93（见 M-1，跨屏一致、非 M5 引入） | ✅（附 M-1 记录） |
| G4 | 图标统一 `--icon-neutral` | 抽查图标计算色 | 浅/深两模式下均存在 `rgb(138,145,158)`（**不随主题变**）；非中性图标色全在语义白名单内 | ✅ |
| G5 | 不使用 Tailwind 内置调色板 | 搜索 `bg-gray-` / `text-black` / `bg-white` 等 | **0 命中** | ✅ |
| G6 | 无意外横向滚动条 | `document.body.scrollWidth <= clientWidth` | 8 屏 × 浅/深 共 16 组，**全部 `scrollWidth == clientWidth`**（含 06 屏缩放视图） | ✅ |
| G7 | 交互元素键盘可达 | Tab 遍历 + 焦点环可见 | 全局 `:focus-visible` 未被覆盖；Composer 的覆盖已修（M5-6）；Tab 面板 aria 接线已补（M5-5） | ✅ |
| G8 | 折叠/展开有过渡，非瞬间跳变 | 目测 + 1-10 中间帧 | `COLLAPSE_DURATION` 180ms；reduced-motion 下 120ms；1-10 中间帧 ≥3 成立；**未给折叠写 `motion-reduce:transition-none`** | ✅ |

---

## 七、5-3 交互打磨五项 · 覆盖盘点（先 grep 再补，只补真缺的）

| 项 | 覆盖对象 | 盘点结论 | 本轮动作 |
|---|---|---|---|
| hover | 17 个含 `onClick` 的组件 | 全部有 `hover:`（或经 headless 注入 / 为自带三态的 IconButton） | **无缺口** |
| active | 同上 | `Button` / `Chip` / `IconButton` / `MenuItem` / `PreviewPane` tab 均有；`Tabs`/`TitleBar` 为 headless/组合豁免（N-3） | **补 1 处**：`MenuItem` 非激活分支（M5-7） |
| 焦点环 | 所有可聚焦元素 | 全局规则存在；**抓到 1 处覆盖**（Composer 的 `outline-none`） | **修 1 处**（M5-6） |
| 空状态 | 空会话 / 空列表 / 无结果搜索 | 空会话：`MessageList` 原本 0 条时渲染空容器（塌陷）→ **补占位**；空列表：04 屏三类分组来自静态 mock 恒非空（N-4）；无结果搜索：原型无此界面（N-4） | **补 1 处**：`empty-state` 占位 + `?empty=1` 正式入口 |
| 长文本 | 超长会话标题 / 工具名 / 路径 | 标题：`TitleBar` 会话标题 → 补 `truncate` + `title`；路径：`Sidebar` 工作目录 → 补 `title`；菜单项：`MenuItem` → 补 `title`（沿用既有 `truncate`） | **补 title 各 1 处**，均满足「省略 + 全称可见」合格线 |

**空状态的复现入口**（正式能力，非临时调试）：

| 参数 | 效果 | 落点 |
|---|---|---|
| `?empty=1` | `loadSession(EMPTY_SESSION)`（`mock/sessions.ts` 新增，0 条消息） | 01 屏消息区渲染 `empty-state` 占位（图标 + 「还没有消息」+ 提示），高度 > 0，不塌陷 |

---

## 八、一句话总结

**8 屏逐屏走查完毕：Blocker 0 条、Major 2 条（`Tabs` aria 接线、`Composer` 焦点环覆盖，均已在本轮修复并复验通过）、
Minor 3 条（1 条记录待后续 + 2 条已修）、Note 4 条（含 2 处「刻意低对比」与 1 处「界面本就不存在」）。**

三条真正守住的底线再次成立：**颜色只有一个来源**（G1 仅 `tokens.css` 命中）、
**深色没有独立代码**（G2 零命中）、**每个偏差都被记录而不是被遗忘**（本清单 9 条差异 + 5 条修复留痕）。

> ⚠️ 本文档由**执行方**撰写，`5-3` 的差异判定结论**待主控复核定稿**。
